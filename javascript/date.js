let d=new Date();
console.log(Date());
console.log(d.toDateString());                  //toDateString() method converts a date to a more readable format
console.log(d.toUTCString());                   //toUTCString() method converts a date to a UTC string
console.log(d.toISOString());                   //toISOString() method converts a date to a string
console.log(d.getFullYear());                   // get year
console.log(d.getMonth());                      // get month
